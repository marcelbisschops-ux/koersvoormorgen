# Koers voor Morgen — Backlog

**Bouwfreeze OPGEHEVEN (Marcel, 31 augustus 2026: "vergeet de freeze").** De freeze van 26 juli
2026 (externe testfase, niets nieuws bouwen) geldt niet meer — backlogpunten mogen gewoon opgepakt
worden. De onderliggende discipline blijft: één wijziging tegelijk, testen vóór opleveren, bij
onduidelijke scope/aanpak eerst afstemmen.

Alles wat afgerond/afgewezen is (t/m 26 augustus 2026) en de langlopende "wacht op jou/jurist"-
punten staan in `BACKLOG-ARCHIEF.md` — die wordt niet standaard meegewogen, alleen op Marcels
verzoek. Dit bestand (`BACKLOG.md`) bevat alleen wat nu daadwerkelijk nog open staat.

Hernummerd op 26 augustus 2026 (op Marcels verzoek) — de oude nummering (1-11, afgeronde en open
punten door elkaar) is opgeschoond; afgeronde punten zijn verplaatst naar het archief.

---

## Openstaande punten

1. **Verkoper zelfregistratie zonder begeleider** (24 augustus 2026) — kan een bedrijf zichzelf als verkoper op het matching-platform zetten, zonder dat er al een adviseur/begeleider bij betrokken is? Antwoord op dit moment: nee — elke verkoper-listing komt altijd via een traject dat door een begeleider is aangemaakt (bewust, het platform draait om M&A-*begeleiding*). Dit is asymmetrisch met de koper-zelfregistratie die al wél bestaat. Symmetrisch maken is een groter, gevoeliger ontwerp — wie wordt dan de begeleider van zo'n traject, welke data mag ongecontroleerd van een anonieme inzending binnenkomen. Bewust nog niet gebouwd.

2. **Automatische bankbetaling voor facturen** (24 augustus 2026) — Marcel expliciet: "uiteindelijk automatisch via directe bankbetaling, nu te kostbaar om te bouwen." Tussenstap staat al live: een downloadbaar PDF-kostenoverzicht per adviseur dat Marcel zelf handmatig kan versturen. Een echte betaalintegratie (bijv. Mollie/Stripe iDEAL) is bewust uitgesteld, geen ingeschatte datum.

3. **Geformaliseerde, doorlopend genummerde BTW-factuur** (24 augustus 2026) — het huidige `/mna/admin/factuur/{gebruiker_id}`-endpoint genereert bewust een "kostenoverzicht", geen officiële BTW-factuur (GOUDEN STANDAARD: geen BTW-percentage of officieel doorlopend factuurnummer verzinnen zonder dat het platform Marcels echte boekhoud-/factuurnummering kent). Nodig vóór dit gebouwd kan worden: BTW-regime/-percentage, en of er een bestaande doorlopende factuurnummerreeks is waar dit platform op moet aansluiten (of dat het platform zijn eigen reeks mag beginnen).

4. **De "M" van M&A — echte fusie-ondersteuning** (25 augustus 2026, Marcel: "platform is nu de A van M&A, wat zou er gebouwd moeten worden om ook het merger deel mee te nemen"). Oorspronkelijk **uitdrukkelijk niet bouwen, alleen vastgelegd** ("Niet bouwen!") — op 27 augustus 2026 heeft Marcel expliciet bevestigd de bouwfreeze en dat "niet bouwen"-besluit te doorbreken, met een bewust beperkte scope: **"1 als basis, 2 waar mogelijk en niet te veel inbreuk."** Vijf structurele gaten, gerangschikt naar omvang — **1 en 3 zijn afgerond, 2 blijft expliciet buiten scope (dat was "te veel inbreuk"), 4 en 5 blijven ongemoeid:**
   1. ~~Waardering — ruilverhouding i.p.v. prijs~~ — **afgerond 27 augustus 2026**: nieuwe functie `dvBerekenRuilverhouding()`/`dvTabelRuilverhouding()` in `mna/03-rekenkern-waardering.js`, aanroepbaar via een nieuwe toggle "Aandelenruil" in het dealvoorstel-formulier (`mna/04-begeleider-dashboard.js`). Waarde-evenredige ruilverhouding op basis van `closing.deelKoperBasis` (verkoperswaarde, al bestaand) tegen een expliciet door de begeleider ingevoerde **externe** koperswaarde — GOUDEN STANDAARD: de koper doorloopt op dit platform geen DD, dus die waarde wordt nooit geschat/berekend, alleen dat lege veld met een verplichte handmatige invoer + duidelijke label "extern aangeleverd, niet geverifieerd". Rekenkundig geverifieerd met een losstaand Node-testscript (4 handmatig doorgerekende cases, unaniem PASS, inclusief een cross-check tussen het percentage en het afgeleide aantal nieuw uit te geven aandelen) én een echte end-to-end run tegen een gevuld testtraject (Fysiopraktijk De Beweging, 🧪 TEST) — resultaat (1,3%/98,7%, 13.133 nieuwe aandelen) rekenkundig consistent bevonden. Bovendien twee permanente regressietests toegevoegd aan `tests/e2e-ui.spec.js` (groep "Rekenkern dealvoorstel", naast de bestaande closing/prijsmechanisme/buy-and-build-tests) — draaien voortaan standaard mee bij `npx playwright test`, geen ad-hoc-script dat na deze sessie verloren gaat. `node --check` groen op alle gewijzigde bestanden; volledige `tests/e2e-ui.spec.js`-suite opnieuw gedraaid (6 van 11 tests lopen zonder ADMIN_KEY, allemaal groen, geen regressie).
   2. **Rolmodel — twee gelijkwaardige partijen i.p.v. koper/verkoper** (grootst/risicovolst) — **blijft bewust buiten scope**, dit is exact het stuk dat Marcel als "te veel inbreuk" aanmerkte: `mna_trajecten` heeft één koper-koppeling en één eenzijdig beoordeelde DD-dataset; een echte fusie vereist dat beide partijen tegelijk DD op elkaar doen als gelijken. Raakt `begeleiderAuth`/`rolVanCode`/alle moduleslots door bijna de hele backend — een herontwerp van een fundament, geen los stuk bouwen. Qua omvang vergelijkbaar met de server-side-module-enforcement-ronde + de cross-path-audit F1-F13 samen.
   3. ~~Aandelenruil/cap table~~ — **afgerond 27 augustus 2026**, samen met punt 1: nieuwe cap table-velden in het dealvoorstel-formulier (aantal bestaande aandelen koper, aantal aandelen verkopende vennootschap — beide optioneel, ter referentie/afgeleide berekening, geen verplichte DD-velden in het sectorprofiel). Bewust géén wijziging aan `mna/01-config-sectorprofielen.js`/de backend-sectorprofielkopie — dit blijft dealvoorstel-specifieke invoer, geen permanente DD-data (voorkomt de bekende sectorprofiel-sync-valkuil uit CLAUDE.md).
   4. **Documenten** (middelgroot tot groot, afhankelijk van scope) — **grotendeels nog open**, met één uitzondering: het gegenereerde dealvoorstel bevat nu een informatief hoofdstuk "Aandelenruil: juridisch vervolgtraject" dat de wettelijke Boek 2 BW-stappen (fusievoorstel, KvK-deponering, verzetstermijn crediteuren, notariële fusieakte) generiek toelicht en expliciet vermeldt dat dit traject die procedure niet doorloopt of vervangt — puur voorlichtend, geen automatische generatie van een juridisch bindende fusieakte (dat blijft altijd bij de notaris). BEM/LoI/SPA zelf zijn niet aangepast en blijven vanuit koop/verkoop-taal geschreven; een **echte juridische fusie** (Boek 2 BW) blijft een apart, groot stuk werk op zichzelf.
   5. **Governance/gezamenlijke besluitvorming** (kleinst) — **nog open**: nu één Opdrachtgever die een bemiddelaar inschakelt; een fusie vereist vaak gezamenlijke besluitvorming vanuit beide besturen — kan mogelijk aanhaken op de bestaande "intern goedgekeurd door"-achtige goedkeuringsflow bij LoI/Bieding.

   Handleiding bijgewerkt in `mna/08-handleiding.js` (stap 4, dealvoorstel-waarderingsmodellen) — geen wijziging nodig in `adv.html`, die bevat deze detailstap niet (adviseursportaal, niet het begeleider-dashboard zelf). Nog lokaal, niet gepusht.

5. **Post-merger integratie (PMI) — earn-out-/vendor-loan-bewaking en 100-dagenplan** (25 augustus 2026, vervolg op punt 4 maar los onderwerp: geldt voor élke deal, niet alleen fusies). **Uitdrukkelijk niet bouwen, alleen vastgelegd.** Het platform stopt nu bewust bij Closing (dataroom-documenten worden 14 dagen na afsluiting verwijderd) — post-merger-tracking vereist het tegenovergestelde: data die juist maanden tot jaren blijft groeien ná de deal. Twee delen zijn verschillend van aard:
   - **Sluit niet aan:** organisatie-integratie, cultuur, systemen samenvoegen — een andere productcategorie (project-/OKR-tooling), geen natuurlijke uitbreiding van een DD-platform.
   - **Sluit wél aan, en is nu een gat:** de rekenkern berekent al earn-out-schema's (`dvBerekenEarnOut`) en vendor loans, en de synergie-aanname die de prijs pre-deal onderbouwde (`dvBerekenSynergie`) — maar er is geen enkel mechanisme om ná closing te bewaken of de earn-out-targets gehaald worden, de vendor loan volgens schema wordt afgelost, of de synergie daadwerkelijk gerealiseerd wordt. De structuur wordt berekend, nooit bewaakt.

   **Ontwerp indien ooit gebouwd:**
   - Nieuwe fase "Integratie" ná Closing, die alleen ontgrendelt bij deals met een earn-out/vendor loan/synergie-aanname.
   - **Earn-out-/vendor-loan-tracking:** nieuwe tabel (bijv. `mna_post_closing_periode`) met per periode een target (al bekend uit de bestaande berekening) tegenover een actual die de koper zelf invult (omzet/EBITDA van die periode). Automatische verdict (op schema/eronder/erboven) + herberekende uitkering via de bestaande `dvBerekenEarnOut`-logica — nu met een echte ingevulde waarde i.p.v. een projectie. Koper vult in, begeleider verifieert/accordeert (zelfde patroon als de bestaande Q&A-goedkeuring). Periodieke e-mailherinnering wanneer een periode moet worden ingevuld (bestaand e-mailpatroon hergebruiken).
   - **100-dagenplan:** een taken-/kanbanlijst per categorie (systemen, personeel, klanten, financiële administratie, branding), met eigenaar/deadline/status — qua patroon bijna identiek aan de al bestaande closing-checklist (`mna_closing_checklist_status`), geen nieuw concept nodig, alleen een nieuwe fase.
   - **Synergie-terugkoppeling:** de pre-deal-aanname naast wat er daadwerkelijk gerealiseerd is, puur informatief, geen betalingsconsequentie.
   - **Rolgevolg:** "koper" is na closing niet meer een kandidaat maar de partij die het bedrijf runt — die rol moet nu actief data invoeren i.p.v. alleen lezen, een echte gedragsverandering van die rol.

   **Architecturaal knelpunt + voorgestelde oplossing (Marcel, 25 augustus 2026):** een traject kan na closing nog 1-3 jaar "leven" voor de earn-out-periode afloopt — botst met de 14-dagenregel en de AVG-verwijderrecht-/archiveringslogica die aanneemt dat een traject snel wordt opgeruimd na afsluiting. Oplossing: **de 14-dagenregel vervalt alleen als de partij expliciet kiest om PMI via het platform te doen** — een bewuste opt-in per traject, geen blanket-beleidswijziging voor alle trajecten. Vereist een eigen rij in privacy.html (nieuw verwerkingsdoel, eigen grondslag/bewaartermijn), naast de bestaande "M&A Begeleiding"-rij die nu nog expliciet bij 14 dagen stopt.

6. ~~Homepage — visuele/positionerings-upgrade~~ — **volledig afgerond 26 augustus 2026** (26 augustus 2026, Marcel deelde een externe (AI-gegenereerde) review van koersvoormorgen.nl). Kernoordeel: structuur/tekst/duidelijkheid waren al sterk, maar de site oogde meer als "nette SaaS-tool" dan als premium M&A-platform — vooral omdat er nergens een echt productbeeld stond. Alle vier deelpunten zijn nu afgerond (lokaal, nog niet gepusht):
   1. ~~Hero-visual boven de vouw~~ — **afgerond 26 augustus 2026**: een echte, met Playwright vastgelegde schermafbeelding van het begeleider-dashboard (documentflow: BEM→teaser→NDA→verkoopmemorandum→bieding→LoI→informatieverzoek) staat nu prominent onder de hero-tekst, in een licht "productvenster"-frame (`.hero-visual`/`.product-frame` in `index.html`).
   2. ~~Premiumere visuele stijl~~ — **afgerond 26 augustus 2026**: grotere/strakkere hero-kop (2.7rem→2.95rem, tighter line-height, `text-wrap:balance`), grotere sectiekoppen (1.7rem→1.85rem) — bewust een aanscherping, geen herontwerp.
   3. ~~Featurekaarten met echte productscreenshots~~ — **afgerond 26 augustus 2026**: i.p.v. alle 8 featuretegels te voorzien van een screenshot (te druk) is gekozen voor één "proof band" direct na de featuregrid (`.proof-band`) met een echte, met echte AI-extractie gevulde Financieel-DD-schermafbeelding + bronvermelding, en een nieuwe eigen sectie "AI die het dossier bewaakt" (`.ai-grid`, 3 concrete voorbeeldkaarten: ontbrekende documenten / afwijking gesignaleerd / actie vereist — pakt meteen ook de eerdere review-suggestie voor een aparte AI-sectie mee). Beide beelden zijn 100% fictief: een nieuw testtraject "🧪 TEST — Fysiopraktijk De Beweging B.V." (production-traject `TNGNKCLR`, tussen_code `TBDKJNHW`) is aangemaakt en met 21 realistische documenten gevuld via de échte `/mna/document/upload`-AI-pijplijn (bronmateriaal: `~/Desktop/testtrajecten-compleet/fysiopraktijk-de-beweging/`), exact volgens de Testdocumenten-standaard. **Bijvangst tijdens het bouwen:** de eerder als "klaar voor gebruik" gedocumenteerde MKB-testtrajecten (restaurant/autodealer/snoepwinkel, zie punt 4 in het archief) bleken alle drie leeg — een admin-actie rond 21-22 augustus had bij alle `🧪 TEST —`-trajecten de documenten/DD-data gewist (nette wijzigingenlog-vermeldingen, dus geen bug, wel een **niet eerder opgemerkt risico**: de naamgevingsconventie maakt geen onderscheid tussen kortlevende verificatietestdata en bewust-blijvende referentiepakketten, dus een volgende opschoonactie kan zomaar weer per ongeluk een pakket als dit fysiopraktijk-traject meepakken. Geen actie ondernomen — hieronder als aandachtspunt genoemd, geen apart genummerd puntje omdat het geen productwijziging betreft.
   4. ~~Scherpere propositie op de homepage~~ — **afgerond 26 augustus 2026** (Marcel: "doe 4"): de twee "scan-card"-blokken (kantoorscan, matching-platform) zijn uit de `#adviseurs`-sectie gehaald — die eindigt nu weer zuiver op de kernpitch van het platform zelf (pills + regiezin). Beide nevenaanbiedingen staan nu in een eigen, bewust lichter gewogen sectie ("Ook van Koers voor Morgen", `.offer-grid`/`.offer-card`) direct daarna: kleinere kaarten, geen zware teal/schaduw-styling meer (voorheen gelijk aan een primaire CTA), duidelijk gelabeld als aanvullend i.p.v. concurrerend met het hoofdproduct. Oude `.scan-card`/`.scan-divider`-CSS verwijderd (nergens anders gebruikt in dit bestand). Geverifieerd in licht + donker thema via lokale Playwright-screenshots.

   (De review noemde ook een aparte "AI die het dossier bewaakt"-sectie met concrete voorbeelden (ontbrekende documenten/afwijkingen/openstaande acties) — inhoudelijk het minst risicovolle deelpunt, kan los van de andere drie als eerste stap worden opgepakt zodra hiermee wordt begonnen.)

7. **Opbrengst-waterfall in het dealvoorstel** (30 augustus 2026, n.a.v. een M&A-onderhandelplaybook dat Marcel deelde) — het dealvoorstel / `mna/03-rekenkern-waardering.js` rekent nu tot ondernemingswaarde (EV) en het koper/verkoper-deel, maar toont nergens de brug van **headline-EV → opbrengst bij closing**. Gecheckt in de code: de netto schuld van het doelwit wordt niet van de koopsom afgetrokken (wordt wél berekend uit de balansvelden, maar alleen gebruikt voor het kengetal netto schuld/EBITDA — `bankLeverage` is nieuwe overnamefinanciering in het schuldafbouwmodel, niet de bestaande schuld van het target); escrow bestaat als parameter (`escrowPct`/`escrowMaanden`, default 12%/18 mnd) maar wordt in geen enkele brug verwerkt; transactiekosten bestaan niet. Er is al een kant-en-klare SVG-waterfallfunctie (`dvSvgWaterfallChart`), nu alleen gekoppeld aan de earn-out-opbouw. Bouwwerk: één blok "Van ondernemingswaarde naar opbrengst bij closing" (tabel + hergebruik van de bestaande waterfallgrafiek), met de brug EV → − netto schuld → − debt-like items → − werkkapitaalcorrectie → − transactiekosten → = equity value → − escrow − uitgestelde earn-out → = cash bij closing → + verwachte vrijval → = verwachte gerealiseerde waarde. Transactiekosten als nieuwe begeleider-parameter (een aanname, net als escrow — nooit gegokt). Valt onder werkregel 13: los rekenscript + handmatig doorgerekende testcase vóór oplevering, plus een permanente regressietest in `tests/e2e-ui.spec.js` (groep "Rekenkern dealvoorstel"). Handleiding bijwerken in `mna/08-handleiding.js` (stap 4); `adv.html` bevat deze detailstap niet.

   **Aanpalende ideeën uit hetzelfde playbook, bewust nog niet ingepland (wachten op Marcels weging):**
   - *Multidimensionale ZOPA-weergave* — de losse knoppen (belang%/behouden belang ≈ rollover, cash% ≈ 100 − earn-out%, earn-out%, escrow%) bestaan al elk met een eigen tabel; wat mist is één gecombineerde "schuif tussen cash nu / rollover / earn-out"-weergave bovenop de waterfall. Kleine uitbreiding ná dit punt, geen apart traject. "Exclusiviteitsduur" is nu geen rekenkern-parameter.
   - *Bod-vergelijker* (Deal Value Matrix: prijs × cash × zekerheid × timing × upside × risico) — bestaat niet. Vereist eerst een datamodel voor meerdere biedingen per traject (`mna_trajecten` heeft nu één koper-koppeling — raakt punt 4.2). Groter, apart.
   - *BATNA / walk-away-voorbereidingsmodule* — een paar reflectievelden voor de verkoper in de strategisch-fase (walk-awayprijs, alternatief zonder verkoop). Klein, laag risico, kan meeliften met een andere strategisch-fase-wijziging.
   - *"Onderhandel de LoI alsof het de SPA is"* — een checklist "kern-economics vroeg vastleggen" gekoppeld aan de bestaande LoI-flow. LoI-generatiemodule nog niet regel-voor-regel gelezen; eerst onderzoeken vóór scoping.

8. **Management-/directie-diepte als waarderings- en PMI-factor** (30 augustus 2026, Marcels eigen inzicht: "directie en management daaronder is key voor de waardering en succesvolle PMI"). Gecheckt in de code: het enige veld dat dit nu raakt is `partner_verandering` ("Veranderbereidheid partners") — vrij tekstveld, gevuld door AI-extractie, getoond in het partnerblok en meegegeven aan de AI-duiding, maar **weegt nergens mee** in de rekenkern of in een risico-oordeel. Het accountancy-profiel heeft géén expliciet veld voor sleutelpersonen/eigenaar-afhankelijkheid (mkb- en software-profiel wél). `strategisch_managementTeam` ("Managementteam na overname") en `strategisch_cultuurFit` ("Cultuuromschrijving + integratieplan") bestaan maar zijn beide níét verplicht. Gaten: geen uitvraag van de diepte van het tweede echelon (managementlaag onder de eigenaar/partners), geen gekwantificeerde key-person-afhankelijkheid, geen retentie-instrumenten voor het MT (de rekenkern koppelt earn-out/lock-up nu alleen aan de verkopende partij zelf), geen expliciete weging van "veranderbereidheid laag / geen tweede echelon" als waarde-drukkende factor in het dealvoorstel.

   **Voorstel, klein → groot:**
   - *Stap 1 — FRONTEND AFGEROND 30 augustus 2026 (bouwfreeze hiervoor bewust opzij gezet door Marcel):* 3 velden toegevoegd aan de `partner`-fase van **alle 4 de sectorprofielen** (Marcel: "geen accountancy-platform, het is breder") — `tweedeEchelon` (fase 1, verplicht), `keyPersonAfhank` (fase 1, verplicht), `mgmtRetentie` (fase 2, optioneel), met een `_hdr_mgmt`-koptekst. Gewijzigd: `mna/01-config-sectorprofielen.js` (velden), `mna/02-state-opslag-documenten.js` (`AI_VELD_MAP` + `setIfEmpty`-regels — geldige maar nog inactieve koppeling tot de backend-schemasleutels bestaan), `mna/04-begeleider-dashboard.js` (informatieverzoek-checklist fase 1 + fase 2, alle 4 profielen), `mna/08-handleiding.js` + `adv.html` (handleiding verkoper + begeleider, werkregel 10). Verificatie: `node --check` groen op alle 4 mna-bestanden + adv.html inline script; structurele assertie (3 velden × 4 profielen, juiste fase/req); volledige `tests/e2e-ui.spec.js` opnieuw gedraaid — 6 passed / 5 skipped (ADMIN_KEY) / 0 failed, geen regressie. **Privacy/VOK (werkregel 17): geen wijziging nodig** — de nieuwe velden vallen onder de al bestaande categorie "due diligence gegevens per fase" in `privacy.html` (r92), zijn geen bijzondere categorie (Art. 9 AVG) en het verwerkingsdoel is ongewijzigd; qua aard identiek aan de al aanwezige velden over partners/personeel (leeftijd, verloop, pensioen, concurrentiebeding sleutelfiguren). Nog **niet gepusht**; itsoftware-fase-1-velden bewust zonder `fase`-key (matcht de conventie van dat profiel).
   - *Stap 1b — BACKEND, LIVE OP PRODUCTIE 30/31 augustus 2026 (Marcel heeft staging + productie gedeployd, Version ID prod 5aa34ec3; geverifieerd: `/health` ok, `GET /mna/sectorprofielen` serveert `tweedeEchelon`/`keyPersonAfhank`/`mgmtRetentie` in alle 4 profielen). Oorspronkelijke code:* in `koersvoormorgen-backend` — `worker/02-config-constanten.js`: 3 sleutels toegevoegd aan `DOC_EXTRACTIE_JSON_SCHEMA_BASIS` (`tweede_echelon`, `key_person_afhankelijkheid`, `management_retentie` — geldt voor álle sectoren, niet via `SECTOR_EXTRACTIE_EXTRA`); `worker/04-ai-extractie.js`: 3 extractie-instructiebullets in `DOC_EXTRACTIE_SYSTEM_PREFIX` (o.a. key_person als percentage 0-100, anders null — nooit gokken); `cloudflare-worker.js`: `DEFAULT_SECTOR_PROFIELEN` opnieuw gegenereerd via `node scripts/sync-sectorprofielen.js --apply` (dry-run bevestigde: alléén de 4 nieuwe velden × 4 profielen, niets anders). `node --check` groen op alle 3 + structurele assertie (alle 4 profielen dragen de velden met juiste fase/req in de geminificeerde kopie). **Deploy kon niet vanuit deze sessie** (geen Cloudflare-credentials, net als git push) — Marcel draait `npx wrangler deploy cloudflare-worker.js --env=staging` + smoke-test, dan productie. **LET OP:** de backend-werkboom bevat ~15 andere ongecommitte bestanden (Marcels eigen werk 21-26 aug: branding-rename KantoorInzicht→Koers voor Morgen, `verstuurExterneMail()`-refactor over 09/12/13/17/20, FK-cascade-migratie in `initDB` (IF NOT EXISTS → geen effect op bestaande prod-DB), adviseur-dossier-export `/adviseur/export/{code}` met eigenaarscheck, volledig-dossier-ZIP bij afsluiten, `/mna/admin/traject/koppel/`, "wis data" tekenen-flag, teaser/memorandum race-condition-fee-fixes, matching-login rate-limit, **VOK Artikel 5 bewaartermijn-herschrijving**). `node --check` groen op de hele worker + alle 24 modules. Coherent en toe te schrijven aan Marcel, maar een deploy stuurt dit alles mee — Marcel checkt vóór productie zelf de VOK-tekstwijziging (werkregel 17) en test de e-mailpaden op staging (de `verstuurExterneMail`-refactor raakt uitnodiging/reset/ondertekening/VOK-mails).
   - *Stap 2 — AFGEROND 31 augustus 2026 (frontend, niet gepusht):* de 3 velden staan nu in het cijferoverzicht van het dealvoorstel (`dvTabelCijferoverzicht()` in `mna/03` — rijen Veranderbereidheid / Tweede echelon / Key-person-afhankelijkheid / Retentieafspraken management), en de dealvoorstel-AI-prompt (`mna/04`, sectie "Cijferoverzicht & interpretatie" + "Retentie & alignment") instrueert de AI expliciet om een lage veranderbereidheid / ontbrekend tweede echelon / hoge key-person-afhankelijkheid als waarde-drukkende risicofactor te benoemen en de managementlaag-retentie in het retentiepakket mee te wegen. `node --check` groen; maatschap-validatie 30/30; e2e 7/0.
   - *Stap 3 — AFGEROND 31 augustus 2026 (frontend, niet gepusht):* nieuwe functies `dvManagementRisico()` + `dvTabelManagementRisico()` in `mna/03`. Kwalitatieve laag/midden/hoog-indicatie afgeleid uit 5 al bestaande DD-velden (key-person-afhankelijkheid % = semi-objectief; tweede echelon / veranderbereidheid / retentieafspraken / opvolging = keyword-heuristiek). Elk aspect dat niet betrouwbaar te beoordelen valt → "onbekend"; ≥3 onbekend → totaaloordeel vervalt ("onvoldoende ingevuld", werkregel 8 — geen gok). Per aspect wordt de ruwe ingevulde waarde meegetoond zodat de basis van het oordeel zichtbaar is. **Raakt nooit de multiple/waardering** — harde disclaimer in de weergave. Verschijnt: op het waarderingsscherm (eigen blok), in het gegenereerde Dealvoorstel (`[TABEL:MGMTRISICO]` + eigen prompt-sectie + regel in de AI-contextblok), en in de CSV-export. Handleiding bijgewerkt (`mna/08` stap 3 + `adv.html` stap 3). Permanente regressietest in `tests/e2e-ui.spec.js` (banding hoog/laag + "onvoldoende ingevuld"-pad) — suite 8 passed / 0 failed; `node --check` groen; maatschap-validatie 30/30.

9. **Maatschap-/eigenaarstructuur + sectorbewuste terminologie** (31 augustus 2026 — Marcel wees erop dat "partner" bij een fysio geen kloppende term is en vroeg of een maatschapstructuur is meegenomen; antwoord was nee).
   - *Increment A — AFGEROND 31 augustus 2026 (frontend, nog niet gepusht):* nieuwe helper `getPartnerTerm()` in `mna/01-config-sectorprofielen.js` (zelfde patroon als `getEigenaarBeloningsVeld()`) — accountancy→"partner(s)", zorg→"maat / praktijkhouder", mkb→"eigenaar / DGA", itsoftware→"founder / eigenaar", onbekende sector→accountancy-term. Toegepast in het partner-op-naam-blok van `mna/06-schermen.js` (verkoper-openingsscherm), `mna/04-begeleider-dashboard.js` (`toonPartnersModal` + navknop `bg-partners` + reconciliatie-waarschuwing) en de handleiding (`mna/08-handleiding.js` stap 7 + `adv.html`). DB-tabel `mna_partners` / endpoint `/mna/partners/` blijven intern zo heten — alleen zichtbare tekst wijzigt. `node --check` groen (4 bestanden + adv.html); `tests/e2e-ui.spec.js` opnieuw gedraaid — 6 passed / 5 skipped / 0 failed.
   - *Increment B — STRUCTURELE MAATSCHAP-ONDERSTEUNING.* **Waarderingsmethode BEVESTIGD door Marcel (31 aug 2026):** bij maatschap/IB-onderneming = genormaliseerde winst ná een marktconform ondernemersloon voor elke werkende maat → sector-multiple daarop → **géén VPB-correctie** in de FCF-keten. De groepsstructuur (`mna_entiteiten`) was BV-only (rollen holding/werkmaatschappij, verplichte KvK, `consolideerFase()` telt optelbare jaarrekeningen op, holding uitgesloten omdat die "al consolideert"). Maatschap past daar niet in: vaak geen eigen KvK/jaarrekening, maten box 1, winst via winstverdeling per maat, hoofdelijke aansprakelijkheid — gangbaar bij fysio/huisarts/tandarts/advocatuur/kleine accountantskantoren. Sub-stappen:
     - *B1 — structuurtype-fundament — AFGEROND 31 augustus 2026 (backend niet gedeployd, frontend niet gepusht):* nieuwe kolom `mna_trajecten.structuur_type TEXT DEFAULT 'bv'` (waarden `bv`/`maatschap`/`eenmanszaak`) — ALTER in `initDB()` (`cloudflare-worker.js`) + in `/adviseur/create` (`worker/16-adviseur.js`), daar **expliciet genormaliseerd, nooit afgeleid uit het optionele vrije `kantoor_rechtsvorm`-veld** (dat zou bij een lege rechtsvorm stilzwijgend `bv`+VPB aannemen voor een maatschap — werkregel 8/13). Komt automatisch mee in de login-respons (`/mna/traject/` doet `SELECT *`). Frontend: `adv.html` aanmaakformulier heeft een "Structuur"-selector die meegaat in de create-body; nieuwe helpers `getStructuurType()` / `isMaatschap()` in `mna/01-config-sectorprofielen.js` (default `bv` = bestaand gedrag). `node --check` groen (5 bestanden + adv.html); e2e-suite 6/0. **Nog te checken:** heeft `marilyn.html` een eigen aanmaakflow die ook de selector nodig heeft?
     - *B2 — winstaandeel-% per maat — AFGEROND 31 augustus 2026 (niet gedeployd/gepusht):* kolom `mna_partners.winstaandeel_pct TEXT` (ALTER in `worker/07-mna-groepen.js` bij GET én POST + centrale CREATE/ALTER in `cloudflare-worker.js` + toegevoegd aan SELECT/INSERT/UPDATE van het `/mna/partners/`-endpoint en aan de dossier-export-dump in `worker/02-config-constanten.js`). Frontend: invoerveld `pt-winstaandeel` / `op-pt-winstaandeel` in het maten-blok van `mna/04` + `mna/06`, **alleen gerenderd bij `isMaatschap()`** (handler leest het veld null-safe, dus geen breuk bij BV-trajecten), plus weergave in de lijst. `node --check` groen; e2e-suite 6/0.
     - *B3 — KvK + consolidatie — AFGEROND 31 augustus 2026 (niet gedeployd):* KvK bij een entiteit was al optioneel in beide frontend-blokken (alleen `if(!naam)` gevalideerd) → geen wijziging nodig. `consolideerFase()` (`cloudflare-worker.js`): haalt nu `structuur_type` op en laat bij `maatschap` de holding-uitsluiting (`e.rol != 'holding'`) weg — de som ís de maatschap. `holdingFilter` is een vaste literal op basis van een boolean (geen user-input in SQL); `trajectId`/`faseId` blijven geparametriseerd. `node --check` groen.
     - *B4 — rekenkern — AFGEROND 31 augustus 2026 (niet gedeployd/gepusht). Marcel: "bouw naar eigen goedvinden" → 3 keuzes gemaakt:* (1) **ondernemersloon = één geaggregeerd bedrag**, overgenomen uit het al bestaande, voor accountancy/zorg verplichte veld eigenaar-/partnerbeloning totaal (`partnerBel` / mkb `dgaSalaris`) — géén nieuw per-maat-veld; begeleider kan "Bewezen EBITDA" in het dealvoorstel handmatig overschrijven voor fijnafstemming. (2) **"werkende maten" niet apart geteld** — het gebruikte bedrag ís al een totaal, dus de slapende-maat-vraag speelt niet. (3) **AI-"second opinion" óók maatschap-bewust gemaakt** (niet alleen de deterministische rekenkern). Implementatie: `mna/03` — `dvGetDefaults()` + `dvBerekenWaardering()` krijgen een maatschap-tak (`multipleType='maatschap'`, grondslag = genormaliseerde winst − eigenaar-/partnerbeloning, `vpbPct=0`; is de beloning leeg → `maatschapGrondslagOnbekend`, w-waardes op `null` i.p.v. een ongecorrigeerd cijfer — werkregel 8); render toont een grondslag-uitleg-banner (of rode waarschuwing), null-safe w-weergave, geen staafgrafiek bij onbekende grondslag; CSV-export toont de grondslag-opbouw. `mna/04` — dealvoorstel- én bieding-modal krijgen dezelfde maatschap-banner/waarschuwing (bedragen komen uit `dvGetDefaults`). Backend `worker/19-info-fases.js` `/mna/waardering/genereer` — trekt bij `structuur_type='maatschap'` de eigenaar-/partnerbeloning van `ebitdaAbs` af (zelfde bron/logica als frontend), prompt + `methode`-label zeggen "winst ná ondernemersloon, geen VpB"; ontbreekt de beloning → 400 met uitleg i.p.v. een gok. **Validatie (werkregel 13):** nieuw `scripts/validate-maatschap-waardering.mjs` — 30 checks over 5 handmatig doorgerekende cases (maatschap bekend/onbekend/negatief geklemd, zorg-maatschap, én **BV-regressie TC3: partnerbeloning NIET afgetrokken, vpbPct 25,8 — exact ongewijzigd**), alle PASS. Permanente regressietest toegevoegd aan `tests/e2e-ui.spec.js` groep "Rekenkern dealvoorstel" (maatschap + BV in één test) — hele suite 7 passed / 0 failed. `node --check` groen op alle gewijzigde front- en backendbestanden.

10. **Testaccounts zien geen pakketprijzen — AFGEROND 31 augustus 2026 (niet gedeployd/gepusht)** (Marcel: "bij testers mogen geen prijzen staan… enkel de prijzen van het koersvoormorgen.nl-pakket, niet in het platform"). Betreft uitsluitend de commerciële platformtarieven (trajectfee/AI-fee/lead-inbreng/meekijker/teaser/verkoopmemorandum/matching) — **niet** de bedragen ín een M&A-traject (waardering/dealvoorstel/bieding blijven volledig zichtbaar). `is_tester` (bestond al op `bf_gebruikers`, deed tot nu toe alleen iets in de uitnodigingsmail) wordt nu meegestuurd in de `/adviseur/trajecten`-respons. `adv.html`: `G.isTester` → de "€ Kosten"-knop en de bijbehorende handleiding-zin worden verborgen voor testaccounts. Backend hard afgeschermd, niet alleen UI: `/adviseur/kosten` geeft voor een tester `{events:[], totaal:0, tarieven:{}, verborgen_tester:true}`; de matching-aanmeldingsnotificatie logt voor een tester geen `platform_fee_event` en noemt geen matching-fee. `node --check` groen; e2e 7/0.

11. **Landingspagina: "vraag een testtraject aan" i.p.v. "plan een demo" via WhatsApp** (Marcel, 31 augustus 2026). Nu staan er op `index.html` drie CTA's `Plan een demo` die naar `wa.me/31638689888` linken. Marcel wil in plaats daarvan een aanvraag voor een **testtraject** (proefaccount) via een **invulformulier** met contactgegevens + KvK-nummer, niet meer via WhatsApp.
    **Beoordeling (Claude):** goede richting. "Demo aanvragen" is vage SaaS-taal; "testtraject aanvragen" sluit aan op hoe onboarding feitelijk werkt (Marcel maakt een testaccount + testtraject aan) en zet de juiste verwachting. Een formulier past beter bij een B2B due-diligence-tool dan WhatsApp (te informeel voor accountants/adviseurs die het platform evalueren) en levert gestructureerde leaddata (naam, kantoor, e-mail, KvK, rol) die Marcel direct kan verwerken; KvK vooraf vragen laat hem vóór toegang verifiëren dat het een echt kantoor is. **Aandachtspunten voor de bouw:** (a) een formulier heeft een ontvangst-endpoint nodig (nieuw worker-endpoint dat een e-mail naar Marcel stuurt, of via `mna_leads`/`lead-aandragen.html`-patroon dat er al is) + spambescherming (Turnstile — er is een `turnstile`-skill/patroon); (b) contactgegevens + KvK = persoonsgegevens → `privacy.html` een regel toevoegen (werkregel 17); (c) overweeg WhatsApp als secundaire optie te laten staan voor wie dat prettiger vindt; (d) bevestigingsscherm/-mail "we nemen contact op".

12. **Homepage desktop-layout opschonen** (Marcel, 31 augustus 2026: "geoptimaliseerd voor mobiel, maar voor desktop erg slordig — die grote tabel aan begin, super lelijk"). **Deel 1 AFGEROND 31 augustus 2026 (niet gepusht):** de "grote tabel" is de `.hero-visual` — `images/product-dashboard.png` is een schermafbeelding van een platte, verticale documentenlijst, op desktop 760px breed op de donkere pagina, gevolgd door een 4,5rem-leegte. Aangepast in `index.html`: op desktop nu `max-width:600px`, hoogte begrensd (`max-height:388px` → alleen de bovenkant zichtbaar) met een `mask-image`-vervaging naar onder, en de leegte eronder van 4,5rem → 2rem. Mobiel expliciet ongemoeid gelaten (`@media(max-width:760px)`: geen hoogtecrop/vervaging, volledige lijst zoals voorheen). Geverifieerd op desktop (1280px): geen horizontale scroll, alle grids intact (feature 2-koloms, ai 3-koloms, proof 2-koloms). **Deel 2 — nog te doen:** Marcel noemde de desktop-layout breder "slordig"; na review van deel 1 bepalen of er meer moet (bijv. hero + productbeeld naast elkaar i.p.v. gecentreerd onder elkaar, strakkere verticale ritmiek).

---

## Testplan: geautomatiseerde end-to-end test (zie `tests/README.md`)

Doel: vóór elke deploy met één commando bevestigen dat het hele systeem werkt.

### Deel A — API-tests (Node-script, `tests/e2e-api.mjs`)
Tegen de live worker, met eigen testdata die het script zelf aanmaakt én opruimt:
1. `/health` — 200 + `ok:true`
2. Adviseur-lifecycle: uitnodigen → activeren → verkoop (limiet/modules) → traject aanmaken → limiet afdwingen → module-gating (traject uit → geblokkeerd) → deactiveren → verwijderen
3. Rollen-login: verkoper-, koper- en tussenpersoonscode geven elk de juiste rol en (voor tussenpersoon) de juiste `modules`
4. Documentupload (multipart, klein testbestand) → analyse aanwezig → `veld_extractie` gevuld → cache-logregel
5. DD-data opslaan (`/mna/save`) en teruglezen
6. Fase-wijziging via logboek-endpoint → `traject_fase` verifiëren → terugzetten
7. Waardering genereren → JSON-structuur valideren
8. Document-e-mailendpoints (nda/loi/bem/dealvoorstel/bieding) naar een testadres → `ok:true` + versie in `mna_doc_versies`
9. Volledige opruiming (testaccount, testtraject incl. documenten via `/admin/delete/mna/`)

AI-afhankelijke stappen (4, 7) krijgen een `--skip-ai` vlag zodat een goedkope snelle run mogelijk is; volledige run kost enkele dubbeltjes aan API-calls.

### Deel B — UI-tests (Playwright, `tests/e2e-ui.spec.js`)
Headless browser tegen lokale mna.html + live worker:
1. Login-schermen: drie rollen, foutmelding bij ongeldige code
2. Begeleider-dashboard: alle knoppen aanwezig; met module `contracten` uit zijn de 6 documentknoppen disabled met de juiste tooltip
3. Dealvoorstel-modal: bekende invoer → **asserten dat de berekende tabelwaarden exact kloppen** (prijsmechanisme, schuldafbouw, buy-and-build) — dit beschermt de rekenkern
4. Bieding-modal: bod = EBITDA × multiple exact; vervolgstappen-paneel verschijnt
5. Informatieverzoek: bestaande knop → fase 1; via bieding-paneel → fase 2 met DD-categorieën
6. Verkoper-flow: inloggen, velden zichtbaar, verversen werkt

### Draaien
```
node tests/e2e-api.mjs            # of: --skip-ai voor snelle run
npx playwright test               # UI-suite
```
Afspraak: beide suites groen vóór elke worker-deploy en vóór elke frontend-push.
